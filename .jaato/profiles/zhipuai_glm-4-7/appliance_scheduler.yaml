# appliance_scheduler — zhipuai set: glm-4.7.
name: appliance_scheduler
inherits: [_base_appliance_scheduler]
plugins: []  # empty keeps the inherited _base surface
model: glm-4.7
provider: zhipuai
plugin_configs:
  zhipuai:
    api_params:
      temperature: 0.8  # creative scheduling across competing loads
      max_tokens: 2000
